# L2 Requirements — Amalgam: Local Monolith Runner

## L1-1: Configuration System

### L2-1.1: YAML Configuration Schema
The tool shall use a YAML configuration file (`amalgam.yml`) at the workspace root defining:
- `repositories`: list of repo entries each with `name`, `type` (microservice | library | plugin | dashboard), `path` (local filesystem path), and optional `enabled` (bool, default true)
- `backend.port`: port for the unified host (default 5000)
- `backend.environment`: dictionary of environment variables
- `frontend.dashboardPath`: path to the dashboard repo
- `frontend.port`: port for the frontend dev server (default 3000)

**Traces to**: L1-1
**Acceptance Criteria**:
- A valid `amalgam.yml` can be deserialized into a strongly-typed C# model
- Missing optional fields use documented defaults
- Invalid YAML produces a clear error message with line number

### L2-1.2: Configuration Validation
The tool shall validate configuration on load: all `path` values must point to existing directories, no duplicate `name` values, at most one `dashboard` type entry, and every `microservice` type must contain a `.csproj` file.

**Traces to**: L1-1
**Acceptance Criteria**:
- Validation errors list all problems (not just the first)
- Each error includes the offending field path and value

### L2-1.3: Init Command Generates Scaffold Config
The `init` command shall scan a given root directory for git repos, detect their type by heuristics (presence of `.csproj`, `package.json`, known folder structures), and generate a starter `amalgam.yml`.

**Traces to**: L1-1
**Acceptance Criteria**:
- Running `amalgam init --scan-dir <path>` produces an `amalgam.yml` with discovered repos
- Detected types are reasonable defaults that the user can manually adjust

---

## L1-2: Local Package Resolution

### L2-2.1: NuGet Local Override
For each `library` repo, the tool shall generate a `Directory.Build.props` or `NuGet.Config` override in each consuming `microservice` repo that redirects the matching NuGet package reference to a local project reference.

**Traces to**: L1-2
**Acceptance Criteria**:
- After running the override, `dotnet build` on a microservice resolves the library from local source, not Nexus
- The original `.csproj` files are not modified; overrides use MSBuild import files

### L2-2.2: npm Local Linking
For each `plugin` repo, the tool shall run `npm link` (or generate workspace-level package resolution) in the `dashboard` project so the plugin resolves to local source.

**Traces to**: L1-2
**Acceptance Criteria**:
- After linking, the dashboard's `node_modules/<plugin-package>` symlinks to the local plugin path
- The dashboard's `package.json` is not permanently modified

---

## L1-3: Unified Backend Host

### L2-3.1: Dynamic Service Loader
The tool shall create an ASP.NET Core host project (`Amalgam.Host`) that dynamically loads each enabled microservice's `Startup`/`Program` registrations (DI services, middleware, controllers) via reflection or a convention-based interface (`IAmalgamModule`).

**Traces to**: L1-3
**Acceptance Criteria**:
- A microservice implementing `IAmalgamModule` has its `ConfigureServices` and `ConfigureApp` methods invoked at host startup
- Controllers from all loaded microservices are discoverable and routable

### L2-3.2: Route Prefix Isolation
Each microservice's routes shall be mounted under a configurable base path prefix (defaulting to `/<service-name>`) to avoid route collisions.

**Traces to**: L1-3
**Acceptance Criteria**:
- Two microservices both defining `GET /health` are reachable at `GET /<svc1>/health` and `GET /<svc2>/health`
- The prefix is configurable per-service in `amalgam.yml`

### L2-3.3: Shared Service Registration
Shared library DI registrations shall be loaded once and made available to all microservices, avoiding duplicate singleton registrations.

**Traces to**: L1-3
**Acceptance Criteria**:
- A shared library registering a singleton `ICache` is resolved as the same instance across all microservices
- No duplicate service registration warnings

---

## L1-4: Frontend Plugin Linking

### L2-4.1: Plugin Link Command
The `link-plugins` command shall iterate configured `plugin` repos and run local linking into the dashboard project.

**Traces to**: L1-4
**Acceptance Criteria**:
- Running `amalgam link-plugins` links all enabled plugin repos
- Running with `--only <name>` links a single plugin

### L2-4.2: Plugin Unlink Command
The `unlink-plugins` command shall restore the dashboard to its published-package state.

**Traces to**: L1-4
**Acceptance Criteria**:
- After unlinking, the dashboard resolves plugins from the registry (npm install restores)

---

## L1-5: CLI Interface

### L2-5.1: CLI Command Structure
The CLI (`amalgam`) shall support these subcommands:
- `init` — generate config
- `validate` — check config
- `build` — build all enabled backend projects
- `run` — start the unified host
- `link-plugins` — link frontend plugins
- `unlink-plugins` — unlink frontend plugins
- `status` — show which services are enabled/discovered

**Traces to**: L1-5
**Acceptance Criteria**:
- `amalgam --help` lists all subcommands with descriptions
- Unknown subcommands produce a helpful error

### L2-5.2: Run Command Options
The `run` command shall support `--port <n>`, `--enable <svc>`, `--disable <svc>`, `--env KEY=VALUE`, and `--verbose` flags to override config at runtime.

**Traces to**: L1-5
**Acceptance Criteria**:
- `--disable SvcA` excludes SvcA even if `enabled: true` in config
- `--env` values are visible to microservice code via `IConfiguration`

---

## L1-6: Developer Experience

### L2-6.1: Startup Health Summary
On startup, the unified host shall print a table listing each service, its status (loaded/skipped/error), route prefix, and load time.

**Traces to**: L1-6
**Acceptance Criteria**:
- The table is printed to stdout before "Now listening on" message
- Services that failed to load show the error reason

### L2-6.2: Configuration Hot-Reload Toggle
The tool shall support `--watch` mode where changes to `amalgam.yml` (specifically `enabled` flags) trigger a restart of only affected services without full process restart.

**Traces to**: L1-6
**Acceptance Criteria**:
- Toggling a service's `enabled` flag while running in `--watch` mode loads/unloads the service within 5 seconds
- A log message confirms the reload

### L2-6.3: Error Diagnostics
When a microservice fails to load (missing dependency, build error, incompatible interface), the tool shall report the root cause with actionable suggestions.

**Traces to**: L1-6
**Acceptance Criteria**:
- A missing project reference error suggests running `amalgam build` first
- A DI registration conflict identifies the conflicting service type and which modules registered it

---

## L1-8: Folder Merge / Assembly

### L2-8.1: Merge Configuration Schema
Each repository entry in `amalgam.yml` shall support an optional `merge` property containing:
- `sources`: list of relative folder paths within the repo to merge (e.g., `src/generated/Shared/Types`, `src/static/templates/Shared`)
- `target`: optional relative path to the `.csproj` or `package.json` within the repo that represents the project being assembled (used to locate the project file in the merged output)

When `merge` is present, the tool assembles the listed source folders into a temp staging directory before any build, package resolution, or linking step.

**Traces to**: L1-8
**Acceptance Criteria**:
- A `RepositoryConfig` with `merge.sources` deserializes correctly from YAML
- Missing `merge` property means no merge step (backward compatible)
- An empty `sources` list produces a validation error

### L2-8.2: Folder Merge Service
The tool shall provide a `FolderMergeService` that:
- Creates a temp staging directory at `.amalgam/merged/<repo-name>/`
- Copies all files from each source folder into the staging directory, preserving relative directory structure within each source
- If the repo has a `.csproj` or `package.json` at the `merge.target` path, copies it to the staging root
- Returns the staging directory path so downstream services (NuGet override, module loader, npm link) use it instead of the original `repo.Path`

**Traces to**: L1-8
**Acceptance Criteria**:
- Files from all source folders appear in the merged output directory
- Overlapping filenames from different sources are handled (last source wins, with a warning logged)
- The staging directory is created under `.amalgam/merged/<repo-name>/` relative to the working directory

### L2-8.3: Merge Integrated into Pipeline
The `amalgam build` and `amalgam run` commands shall automatically execute the merge step for any repository with a `merge` configuration before building or loading. The resolved (merged) path shall be used for all downstream operations (NuGet override generation, module loading, npm linking).

**Traces to**: L1-8
**Acceptance Criteria**:
- Running `amalgam build` on a repo with `merge` config merges folders first, then builds from the staging directory
- Running `amalgam run` uses the merged path for module loading
- `amalgam link-plugins` uses the merged path if the plugin has a merge config

### L2-8.4: Merge Clean Command
The `amalgam merge-clean` command shall delete all staging directories under `.amalgam/merged/`.

**Traces to**: L1-8
**Acceptance Criteria**:
- Running `amalgam merge-clean` removes the `.amalgam/merged/` directory
- Running it when no merged directories exist produces no error

### L2-8.5: Merge Validation
The config validator shall validate merge configurations:
- All `sources` paths must exist as directories within the repo path
- The `target` path (if specified) must exist within one of the source folders

**Traces to**: L1-8
**Acceptance Criteria**:
- A nonexistent source path produces a validation error naming the repo and missing path
- Validation errors are collected alongside other config validation errors

---

## L1-7: .NET Tool Packaging and NuGet Publishing

### L2-7.1: .NET Tool csproj Metadata
The `Amalgam.Cli.csproj` shall include all properties required for .NET global tool packaging:
- `PackAsTool` = true
- `ToolCommandName` = "amalgam"
- `PackageId`, `Version`, `Authors`, `Description`, `PackageReadmeFile`, `PackageLicenseExpression`
- `PackageProjectUrl`, `RepositoryUrl` (placeholder values acceptable)

**Traces to**: L1-7
**Acceptance Criteria**:
- `dotnet pack` on Amalgam.Cli produces a `.nupkg` file in the output directory
- The `.nupkg` contains the tool shim with command name "amalgam"
- The package includes a README and license metadata

### L2-7.2: NuGet Publish Support
The project shall include a CI-friendly publish workflow: `dotnet nuget push` can push the `.nupkg` to nuget.org using an API key.

**Traces to**: L1-7
**Acceptance Criteria**:
- `dotnet pack -c Release` produces a release-mode `.nupkg`
- The package can be pushed to nuget.org via `dotnet nuget push`
- A user can install with `dotnet tool install -g Amalgam.Cli` and then run `amalgam --help`

### L2-7.3: Package Validation
The `.nupkg` shall be valid and installable as a global tool locally before publishing.

**Traces to**: L1-7
**Acceptance Criteria**:
- `dotnet tool install -g --add-source ./nupkg Amalgam.Cli` succeeds from the local pack output
- Running `amalgam --help` after install shows the expected subcommands
- `dotnet tool uninstall -g Amalgam.Cli` cleanly removes the tool

---

## L1-10: Configuration Management API

### L2-10.1: API Project Setup
The solution shall include a new ASP.NET Core Web API project (`Amalgam.Api`) that references `Amalgam.Core`. It shall configure CORS for local Angular development, use System.Text.Json for serialization with camelCase property naming, and register Core services in DI.

**Traces to**: L1-10
**Acceptance Criteria**:
- The project builds and starts, returning 200 on `GET /api/health`
- CORS allows requests from `http://localhost:4200`
- JSON responses use camelCase property names

### L2-10.2: Configuration CRUD Endpoints
The API shall expose endpoints to manage the full `amalgam.yml` configuration:
- `GET /api/config` — returns the current configuration
- `PUT /api/config` — replaces the full configuration and saves to disk
- `GET /api/config/yaml` — returns the raw YAML string
- `PUT /api/config/yaml` — accepts a raw YAML string, parses, validates, and saves

**Traces to**: L1-10
**Acceptance Criteria**:
- `GET /api/config` returns the deserialized configuration as JSON
- `PUT /api/config` saves a valid config and returns 200; returns 400 with validation errors for invalid config
- `GET /api/config/yaml` returns the YAML as a plain text string
- `PUT /api/config/yaml` parses YAML, validates, saves, and returns 200; returns 400 for invalid YAML

### L2-10.3: Repository CRUD Endpoints
The API shall expose endpoints to manage individual repositories:
- `GET /api/repositories` — list all repositories
- `GET /api/repositories/{name}` — get a single repository by name
- `POST /api/repositories` — add a new repository
- `PUT /api/repositories/{name}` — update an existing repository
- `DELETE /api/repositories/{name}` — remove a repository
- `PATCH /api/repositories/{name}/toggle` — toggle the enabled flag

**Traces to**: L1-10
**Acceptance Criteria**:
- `GET /api/repositories` returns all repositories as a JSON array
- `POST /api/repositories` with a duplicate name returns 409 Conflict
- `PUT /api/repositories/{name}` for a non-existent name returns 404
- `DELETE /api/repositories/{name}` removes the repo and saves config
- `PATCH /api/repositories/{name}/toggle` flips the enabled flag and saves

### L2-10.4: Validation Endpoint
The API shall expose `POST /api/config/validate` that runs the ConfigValidator against the current (or provided) configuration and returns a structured validation result with `isValid` boolean and `errors` array.

**Traces to**: L1-10
**Acceptance Criteria**:
- A valid configuration returns `{ "isValid": true, "errors": [] }`
- An invalid configuration returns `{ "isValid": false, "errors": ["..."] }` with all errors listed

### L2-10.5: Directory Scanning Endpoint
The API shall expose `POST /api/scan` accepting a `{ "path": "..." }` body that runs DirectoryScanner and returns discovered repositories.

**Traces to**: L1-10
**Acceptance Criteria**:
- Scanning a directory with repos returns a list of discovered RepositoryConfig objects
- Scanning a non-existent path returns 400 with an error message

### L2-10.6: Directory Browsing Endpoint
The API shall expose `GET /api/directories?path=...&prefix=...` that lists subdirectories at a given path for type-ahead support. If `prefix` is provided, results are filtered to directories whose name starts with the prefix.

**Traces to**: L1-10
**Acceptance Criteria**:
- `GET /api/directories?path=C:/projects` returns subdirectory names
- Non-existent paths return an empty array (not an error)
- Results are filtered by prefix when provided

### L2-10.7: Templates Endpoint
The API shall expose `GET /api/templates` returning a list of pre-built configuration templates, and `GET /api/templates/{id}` returning a full AmalgamConfig for a specific template.

**Traces to**: L1-10
**Acceptance Criteria**:
- `GET /api/templates` returns at least 3 templates each with id, name, description, and repositoryCount
- `GET /api/templates/{id}` returns a full configuration that can be used as a starting point
- `GET /api/templates/unknown` returns 404

### L2-10.8: Dashboard Summary Endpoint
The API shall expose `GET /api/dashboard` returning a summary object with repository counts by type, validation status, and total count for the dashboard screen.

**Traces to**: L1-10
**Acceptance Criteria**:
- Response includes `totalRepositories`, `countByType` (object with Microservice/Library/Plugin/Dashboard counts), and `validation` (isValid + error count)

---

## L1-9: Web Application for Configuration Management

### L2-9.1: Responsive App Shell & Navigation
The app shall have a responsive shell with Angular Material sidenav for navigation. On mobile (≤599px), navigation collapses to a hamburger menu. On tablet (600–1279px), navigation shows as a rail with icons. On desktop (≥1280px), navigation is a full sidebar. Navigation items: Dashboard, Repositories, Backend Config, Frontend Config, Templates, and Settings.

**Traces to**: L1-9
**Acceptance Criteria**:
- Mobile layout shows hamburger menu with bottom navigation bar
- Tablet layout shows icon rail on the left
- Desktop layout shows full sidebar with labels and icons
- All three breakpoints are designed as separate screens

### L2-9.2: Dashboard / Home Screen
The dashboard shall show an overview of the current configuration: total repositories by type (with mat-chips), validation status (pass/fail with error count), quick-action FABs for "New Repository" and "Run Wizard", and recent activity or change history cards.

**Traces to**: L1-9
**Acceptance Criteria**:
- Dashboard displays repository count cards grouped by type (Microservice, Library, Plugin, Dashboard)
- Validation status banner shows pass (green) or fail (red/amber) with error count
- Quick action buttons are visible and accessible
- Layout adapts across mobile, tablet, and desktop

### L2-9.3: Repository List & Management
The app shall display repositories in a responsive list/table with columns for name, type (color-coded chip), path, enabled toggle, and actions (edit, clone, delete). On mobile, the list collapses to cards. Filtering by type and search by name shall be supported.

**Traces to**: L1-9
**Acceptance Criteria**:
- Desktop shows a mat-table with sortable columns
- Mobile shows mat-card list with key info and swipe actions
- Type filter chips and search bar are present
- Enable/disable toggle is inline

### L2-9.4: Repository Add/Edit Form with Validation
The repository form shall use Angular Material form fields with real-time validation. Fields: name (required, unique, alphanumeric+hyphens), type (mat-select), path (with browse/type-ahead), enabled (slide toggle), routePrefix (auto-suggested from name), packageName (conditional on type=Plugin). All validation errors display as mat-error with descriptive messages below each field.

**Traces to**: L1-9
**Acceptance Criteria**:
- Each field shows specific validation error messages (e.g., "Name must be unique", "Path does not exist")
- Type-ahead for path field suggests directory paths
- Route prefix auto-populates as `/<name>` but is editable
- Package name field only appears when type is Plugin
- Form has clear Save and Cancel actions

### L2-9.5: Merge Configuration Section
Within the repository form, when a user adds merge config, a sub-section appears with: a dynamic list of source paths (add/remove with type-ahead), an optional target path field, and validation that sources are not empty. Descriptive help text explains what merge does.

**Traces to**: L1-9
**Acceptance Criteria**:
- "Add Merge Configuration" expansion panel appears within repo form
- Source paths can be added/removed dynamically
- Empty sources list shows validation error
- Help tooltip or info banner explains merge behavior

### L2-9.6: Backend & Frontend Configuration Screens
Separate screens for backend config (port number with validation 1–65535, environment variables as key-value pairs with add/remove) and frontend config (dashboard path with type-ahead, port). Environment variables use a dynamic form array with mat-form-fields.

**Traces to**: L1-9
**Acceptance Criteria**:
- Backend config screen shows port field and environment variable editor
- Port validates range 1–65535 with error message
- Environment variables support add/remove with key-value inputs
- Frontend config shows dashboardPath and port fields

### L2-9.7: Alerts, Dialogs & Notifications
The app shall include designs for: (a) mat-dialog for delete confirmation with warning icon and action buttons, (b) mat-dialog for clone configuration with name input, (c) mat-snackbar notifications for success/error/info, (d) inline alert banners (warning, error, info, success) using mat-card with colored left border and icon.

**Traces to**: L1-9
**Acceptance Criteria**:
- Delete confirmation dialog shows warning icon, item name, and Confirm/Cancel buttons
- Clone dialog shows source name, new name input with validation, and Clone/Cancel buttons
- Snackbar notifications appear at bottom with dismiss action
- Inline alert banners show for validation errors, save success, and informational notices

### L2-9.8: Configuration Wizard (New Config)
A multi-step wizard using mat-stepper shall guide users through creating a new configuration: Step 1 – Scan directory or start blank, Step 2 – Review/edit discovered repositories, Step 3 – Configure backend settings, Step 4 – Configure frontend settings, Step 5 – Review & save. Each step validates before allowing next.

**Traces to**: L1-9
**Acceptance Criteria**:
- Wizard uses horizontal stepper on desktop, vertical on mobile
- Each step shows validation state (complete, error, pending)
- "Scan Directory" step includes path input with type-ahead
- Review step shows full YAML preview
- Back/Next/Finish buttons are clearly visible

### L2-9.9: Templates & Configuration Cloning
The app shall include a Templates screen listing pre-built configuration templates (e.g., "Basic Microservice Setup", "Full Stack with Plugins", "Library Development"). Each template shows a description and "Use Template" button. Configuration cloning shall allow duplicating an existing config with a new name, modifying the clone independently.

**Traces to**: L1-9
**Acceptance Criteria**:
- Templates screen shows template cards with name, description, and repo count preview
- "Use Template" opens the wizard pre-filled with template data
- Clone action available from repository list and config overview
- Clone dialog validates new name uniqueness

### L2-9.10: YAML Preview & Export
The app shall include a YAML preview panel (using a code-style monospace display) that shows the live configuration as valid YAML. Users can toggle between visual editor and YAML view. An export/download button saves the YAML file.

**Traces to**: L1-9
**Acceptance Criteria**:
- YAML preview updates in real-time as configuration changes
- Toggle between visual editor and YAML view is accessible
- Download button exports `amalgam.yml`
- YAML preview uses monospace font with syntax highlighting colors

### L2-9.11: Dark Mode Default Theme
The web application shall use a dark mode color scheme by default across all screens and components. The dark theme shall use a dark background (e.g., #0F172A or similar dark slate), lighter surface colors for cards and containers, light text on dark backgrounds, and adjusted accent/status colors that maintain sufficient contrast (WCAG AA). All reusable components (buttons, inputs, chips, toggles, cards, alerts, dialogs, snackbars, navigation) shall be updated to use dark mode variables. The design variables shall be updated so that all existing screens automatically reflect the dark theme.

**Traces to**: L1-9
**Acceptance Criteria**:
- All screens (mobile, tablet, desktop) render with dark backgrounds and light text
- Cards and surface containers use a slightly lighter dark shade than the background
- Input fields have visible borders and light placeholder/value text on dark backgrounds
- Alert banners maintain distinct color coding (error, success, warning, info) with dark-adjusted tints
- Navigation elements (sidebar, icon rail, bottom nav) use dark surface colors
- Chip colors for repository types remain distinguishable on dark backgrounds
- Toggle switches remain clearly visible in on/off states
- All text meets WCAG AA contrast ratio against its background

---

## L1-11: Frontend Components Library

### L2-11.1: Components Library Project Setup
The Angular workspace shall include a library project (`components`) generated with `ng generate library components`. The library shall export all public components via its public API. The library shall use Angular Material components as building blocks.

**Traces to**: L1-11
**Acceptance Criteria**:
- Library project exists at `projects/components` with proper `ng-package.json`
- Library builds successfully with `ng build components`
- All unit tests pass with `ng test components`
- `tsconfig.json` has path mapping for `@amalgam/components`

### L2-11.2: App Shell Component
A stateless `AppShellComponent` that provides the responsive application shell layout with a Material sidenav container. Inputs: `sidenavMode` (side/over/push), `sidenavOpened` (boolean), `navigationItems` (array of {icon, label, route}). Outputs: `sidenavToggle`, `navigationItemClick`.

**Traces to**: L1-11
**Acceptance Criteria**:
- Component renders sidenav container with toolbar
- Sidenav mode and opened state are driven by inputs
- Navigation items render with icons and labels
- Unit tests cover all input combinations and output events
- Line coverage ≥ 95%

### L2-11.3: Navigation Components
Stateless navigation components: `SidebarNavComponent` (full desktop sidebar with icons + labels), `IconRailComponent` (tablet icon-only rail), `BottomNavComponent` (mobile bottom navigation bar). All accept `navigationItems` input and emit `itemClick` output.

**Traces to**: L1-11
**Acceptance Criteria**:
- Each component renders the correct Material navigation pattern
- Active item is visually indicated via `activeRoute` input
- Unit tests cover rendering, active state, and click events
- Line coverage ≥ 95%

### L2-11.4: Dashboard Card Components
Stateless card components: `StatCardComponent` (displays a label, count, and icon), `ValidationBannerComponent` (shows pass/fail status with error count). Inputs are primitive data types. No API calls.

**Traces to**: L1-11
**Acceptance Criteria**:
- StatCardComponent renders label, count, and icon from inputs
- ValidationBannerComponent shows green/red based on `isValid` input
- Unit tests verify rendering for all input variations
- Line coverage ≥ 95%

### L2-11.5: Repository List Components
Stateless list/table components: `RepoTableComponent` (desktop mat-table with columns), `RepoCardListComponent` (mobile card list). Inputs: `repositories` array, `sortColumn`, `sortDirection`. Outputs: `edit`, `delete`, `clone`, `toggleEnabled`, `sort`.

**Traces to**: L1-11
**Acceptance Criteria**:
- Table renders with name, type (chip), path, enabled toggle, action buttons
- Card list renders cards with key repository info
- Type chips use color coding (Microservice=blue, Library=green, Plugin=purple, Dashboard=orange)
- All events emit with the correct repository data
- Line coverage ≥ 95%

### L2-11.6: Form Field Components
Stateless form components wrapping Angular Material: `PathInputComponent` (text input with type-ahead suggestions dropdown), `TypeSelectComponent` (mat-select for RepositoryType), `PortInputComponent` (number input with range validation display), `EnvVarEditorComponent` (key-value list with add/remove). All use ControlValueAccessor for form integration.

**Traces to**: L1-11
**Acceptance Criteria**:
- Each component implements ControlValueAccessor
- PathInputComponent shows suggestions via `suggestions` input and emits `searchChange`
- TypeSelectComponent renders all 4 repository types
- PortInputComponent displays validation error messages via `errors` input
- EnvVarEditorComponent supports dynamic add/remove of key-value pairs
- Line coverage ≥ 95%

### L2-11.7: Dialog Components
Stateless dialog content components: `ConfirmDialogComponent` (title, message, confirm/cancel buttons with warning icon), `CloneDialogComponent` (source name display, new name input, clone/cancel buttons). Accept data via MAT_DIALOG_DATA injection.

**Traces to**: L1-11
**Acceptance Criteria**:
- ConfirmDialogComponent renders warning icon, title, message, and action buttons
- CloneDialogComponent renders source name and validates new name input
- Dialog results return appropriate data on confirm/cancel
- Line coverage ≥ 95%

### L2-11.8: Feedback Components
Stateless feedback components: `AlertBannerComponent` (inline alert with type: error/success/warning/info, icon, message, optional dismiss), `NotificationComponent` (snackbar content with message and action). Input-driven, no side effects.

**Traces to**: L1-11
**Acceptance Criteria**:
- AlertBannerComponent renders with correct icon and color per type
- AlertBannerComponent emits `dismiss` event
- NotificationComponent renders message and action button
- Line coverage ≥ 95%

### L2-11.9: Wizard Step Components
Stateless wizard presentation components: `WizardStepperComponent` wrapping mat-stepper with step state indicators. Inputs: `steps` array with label/status/content, `orientation` (horizontal/vertical), `currentStep`. Outputs: `stepChange`.

**Traces to**: L1-11
**Acceptance Criteria**:
- Renders mat-stepper with correct orientation
- Step labels and completion states render from inputs
- Step navigation emits stepChange
- Line coverage ≥ 95%

### L2-11.10: YAML Preview Component
Stateless `YamlPreviewComponent` displaying YAML text in a monospace code block. Inputs: `yamlContent` (string), `showLineNumbers` (boolean). Outputs: `download`, `toggleView`.

**Traces to**: L1-11
**Acceptance Criteria**:
- Renders YAML content in monospace font
- Optional line numbers displayed
- Download and toggle buttons emit events
- Line coverage ≥ 95%

---

## L1-12: Frontend Domain Library

### L2-12.1: Domain Library Project Setup
The Angular workspace shall include a library project (`domain`) generated with `ng generate library domain`. The library depends on `@amalgam/components` and `@amalgam/api`. It exports smart/container components that orchestrate state and API calls.

**Traces to**: L1-12
**Acceptance Criteria**:
- Library project exists at `projects/domain` with proper `ng-package.json`
- Library builds successfully with `ng build domain`
- All unit tests pass with `ng test domain`
- `tsconfig.json` has path mapping for `@amalgam/domain`

### L2-12.2: Dashboard Container Component
`DashboardContainerComponent` loads dashboard data from the API service, manages loading/error states, and passes data to presentation components (StatCardComponent, ValidationBannerComponent). Provides quick actions (navigate to new repo, run wizard).

**Traces to**: L1-12
**Acceptance Criteria**:
- Calls DashboardService.getSummary() on init
- Displays loading spinner while data loads
- Passes data to StatCardComponent and ValidationBannerComponent
- Quick action buttons navigate to correct routes
- Error state shows alert banner
- Unit tests mock API service and verify all states
- Line coverage ≥ 95%

### L2-12.3: Repository List Container Component
`RepositoryListContainerComponent` loads repositories from API, manages filtering by type, search by name, sorting, and delegates CRUD actions. Opens dialogs for delete confirmation and clone.

**Traces to**: L1-12
**Acceptance Criteria**:
- Loads repositories via RepositoryService on init
- Filters by type using chip selection
- Searches by name with debounced input
- Sort delegates to the table component
- Delete opens ConfirmDialog then calls API
- Clone opens CloneDialog then calls API
- Toggle calls API toggle endpoint
- Snackbar notifications on success/error
- Unit tests mock all API calls and dialog interactions
- Line coverage ≥ 95%

### L2-12.4: Repository Form Container Component
`RepositoryFormContainerComponent` manages the add/edit repository form. Loads existing repo data for edit mode. Validates in real-time. Calls directory browsing API for path type-ahead. Auto-generates route prefix from name. Conditionally shows package name field for Plugin type and merge config section.

**Traces to**: L1-12
**Acceptance Criteria**:
- Edit mode loads existing repository data from API
- Add mode starts with empty form
- Path type-ahead calls DirectoryService with debounce
- Route prefix auto-populates from name field
- Package name field visibility toggles with type selection
- Merge section expands/collapses with dynamic source paths
- Form validation prevents save with invalid data
- Save calls create or update API endpoint
- Success navigates back to repository list
- Unit tests cover add mode, edit mode, validation, and API interactions
- Line coverage ≥ 95%

### L2-12.5: Backend Config Container Component
`BackendConfigContainerComponent` loads and saves backend configuration (port, environment variables). Validates port range 1-65535. Manages dynamic environment variable form array.

**Traces to**: L1-12
**Acceptance Criteria**:
- Loads backend config from API on init
- Port field validates range 1-65535
- Environment variables support add/remove
- Save calls API and shows success/error notification
- Unit tests mock API and verify validation
- Line coverage ≥ 95%

### L2-12.6: Frontend Config Container Component
`FrontendConfigContainerComponent` loads and saves frontend configuration (dashboard path with type-ahead, port). Calls directory API for path suggestions.

**Traces to**: L1-12
**Acceptance Criteria**:
- Loads frontend config from API on init
- Dashboard path has type-ahead via DirectoryService
- Port field validates range
- Save calls API and shows notification
- Line coverage ≥ 95%

### L2-12.7: Config Wizard Container Component
`ConfigWizardContainerComponent` manages the multi-step configuration wizard: Step 1 (scan or blank), Step 2 (review repos), Step 3 (backend config), Step 4 (frontend config), Step 5 (review YAML & save). Orchestrates API calls for scanning and saving.

**Traces to**: L1-12
**Acceptance Criteria**:
- Step 1 calls ScanService when user provides path
- Step 2 shows discovered repos with edit capability
- Steps 3-4 collect backend/frontend config
- Step 5 shows YAML preview
- Finish saves entire config via API
- Step validation prevents forward navigation on invalid data
- Horizontal stepper on desktop, vertical on mobile (uses BreakpointObserver)
- Unit tests cover each step and wizard flow
- Line coverage ≥ 95%

### L2-12.8: Templates Container Component
`TemplatesContainerComponent` loads template list from API, displays template cards, and handles "Use Template" action by navigating to wizard pre-filled with template data.

**Traces to**: L1-12
**Acceptance Criteria**:
- Loads templates via TemplateService on init
- Renders template cards with name, description, repo count
- "Use Template" loads full template config and navigates to wizard
- Loading and error states handled
- Unit tests mock API
- Line coverage ≥ 95%

### L2-12.9: YAML Editor Container Component
`YamlEditorContainerComponent` manages the YAML view/edit mode. Loads YAML from API, allows editing, validates on save, and toggles between visual and YAML views. Provides download functionality.

**Traces to**: L1-12
**Acceptance Criteria**:
- Loads YAML content from ConfigService
- Toggle switches between visual editor route and YAML view
- Save validates YAML via API
- Download exports as `amalgam.yml` file
- Error display for invalid YAML
- Line coverage ≥ 95%

### L2-12.10: Responsive Layout Container
`LayoutContainerComponent` uses Angular CDK BreakpointObserver to detect viewport size and switches between mobile/tablet/desktop navigation layouts. Manages sidenav state and delegates to appropriate navigation component.

**Traces to**: L1-12
**Acceptance Criteria**:
- Mobile (≤599px): hamburger menu + bottom nav
- Tablet (600-1279px): icon rail sidebar
- Desktop (≥1280px): full sidebar with labels
- Sidenav toggle works correctly
- Unit tests mock BreakpointObserver for each breakpoint
- Line coverage ≥ 95%

---

## L1-13: Frontend API Library

### L2-13.1: API Library Project Setup
The Angular workspace shall include a library project (`api`) generated with `ng generate library api`. The library exports service classes and TypeScript interfaces for all API models. It provides an `ApiModule` (or `provideApi()` function) for configuration with base URL.

**Traces to**: L1-13
**Acceptance Criteria**:
- Library project exists at `projects/api` with proper `ng-package.json`
- Library builds successfully with `ng build api`
- All unit tests pass with `ng test api`
- `tsconfig.json` has path mapping for `@amalgam/api`
- Base URL is configurable via injection token

### L2-13.2: API Model Interfaces
The library shall export TypeScript interfaces matching the backend API models: `AmalgamConfig`, `RepositoryConfig`, `RepositoryType` (union type), `BackendConfig`, `FrontendConfig`, `MergeConfig`, `ValidationResult`, `DashboardSummary`, `TemplateSummary`, `TemplateInfo`, `ScanRequest`, `YamlRequest`.

**Traces to**: L1-13
**Acceptance Criteria**:
- All interfaces match the backend API response shapes exactly
- RepositoryType is `'Microservice' | 'Library' | 'Plugin' | 'Dashboard'`
- Interfaces are exported from the library's public API
- Unit tests verify interface compatibility (type checking)

### L2-13.3: Config API Service
`ConfigService` with methods: `getConfig(): Observable<AmalgamConfig>`, `updateConfig(config): Observable<AmalgamConfig>`, `getYaml(): Observable<string>`, `updateYaml(yaml): Observable<AmalgamConfig>`, `validate(config?): Observable<ValidationResult>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Each method calls the correct HTTP endpoint with proper method and body
- Error responses are mapped to typed error objects
- Unit tests use HttpTestingController to verify all requests
- Line coverage ≥ 95%

### L2-13.4: Repository API Service
`RepositoryService` with methods: `getAll(): Observable<RepositoryConfig[]>`, `getByName(name): Observable<RepositoryConfig>`, `create(repo): Observable<RepositoryConfig>`, `update(name, repo): Observable<RepositoryConfig>`, `delete(name): Observable<void>`, `toggle(name): Observable<RepositoryConfig>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Each method calls the correct endpoint (GET/POST/PUT/DELETE/PATCH)
- 404 errors are handled distinctly from other errors
- 409 conflicts are surfaced with meaningful messages
- Unit tests verify all HTTP interactions
- Line coverage ≥ 95%

### L2-13.5: Dashboard API Service
`DashboardService` with method: `getSummary(): Observable<DashboardSummary>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Calls GET /api/dashboard
- Returns typed DashboardSummary
- Unit tests verify request and response mapping
- Line coverage ≥ 95%

### L2-13.6: Directory API Service
`DirectoryService` with method: `browse(path, prefix?): Observable<string[]>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Calls GET /api/directories with query params
- Returns string array of directory names
- Unit tests verify query parameter encoding
- Line coverage ≥ 95%

### L2-13.7: Scan API Service
`ScanService` with method: `scan(path): Observable<RepositoryConfig[]>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Calls POST /api/scan with path body
- Returns array of discovered RepositoryConfig
- 400 errors surfaced with message
- Unit tests verify request/response
- Line coverage ≥ 95%

### L2-13.8: Template API Service
`TemplateService` with methods: `getAll(): Observable<TemplateSummary[]>`, `getById(id): Observable<TemplateInfo>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- getAll calls GET /api/templates
- getById calls GET /api/templates/{id}
- 404 handled for unknown template
- Unit tests verify all interactions
- Line coverage ≥ 95%

### L2-13.9: Health API Service
`HealthService` with method: `check(): Observable<{status: string}>`.

**Traces to**: L1-13
**Acceptance Criteria**:
- Calls GET /api/health
- Returns health status object
- Unit test verifies request
- Line coverage ≥ 95%

---

## L1-14: Frontend Application Testing

### L2-14.1: Playwright Setup and Page Object Model Base
The amalgam application shall include Playwright test configuration with a base `PageObject` class providing common navigation and interaction methods. Page objects shall abstract DOM selectors from test logic.

**Traces to**: L1-14
**Acceptance Criteria**:
- Playwright config exists with proper base URL and browser settings
- Base PageObject class with `goto()`, `waitForLoad()`, common helpers
- Page objects are in a dedicated `e2e/pages/` directory

### L2-14.2: Boundary Interface Tests with Mocked API
Playwright tests that mock all API responses using `page.route()` to intercept HTTP calls. Tests verify UI behavior in isolation without a running backend. Cover: dashboard loading, repository list/filter/search, add/edit repository form, delete/clone dialogs, backend/frontend config screens, wizard flow, templates, YAML preview.

**Traces to**: L1-14
**Acceptance Criteria**:
- All API calls are intercepted and mocked
- Tests run without a backend server
- Dashboard test verifies stat cards render with mocked data
- Repository list test verifies filtering, searching, and CRUD operations
- Form tests verify validation messages and field behavior
- Wizard test verifies multi-step flow
- Tests use Page Object Model pattern exclusively

### L2-14.3: End-to-End Tests with Real Backend
Playwright tests that run against a live Amalgam.Api backend. Cover the primary user flows: view dashboard, add a repository, edit a repository, delete a repository, toggle enabled, run wizard, use template, view/edit YAML.

**Traces to**: L1-14
**Acceptance Criteria**:
- Tests require a running Amalgam.Api backend
- Tests use Page Object Model pattern
- Tests clean up created data after each test
- Primary CRUD flows are covered
- Tests run in CI with a pre-started backend
